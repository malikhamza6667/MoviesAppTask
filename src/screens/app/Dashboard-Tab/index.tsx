import { ScreenWrapper } from "@components"
import { Text, View } from "react-native"

const Dashboard=()=>{

    return(
        <ScreenWrapper>
            <Text>This is the Dashboard no screen added on Figma</Text>
        </ScreenWrapper>
    )
}

export default Dashboard