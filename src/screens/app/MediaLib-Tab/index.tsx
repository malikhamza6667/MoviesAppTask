import { ScreenWrapper } from "@components"
import { Text, View } from "react-native"

const MediaLibrary=()=>{

    return(
        <ScreenWrapper>
        <Text>This is the Media Library no screen added on Figma</Text>
    </ScreenWrapper>
    )
}

export default MediaLibrary