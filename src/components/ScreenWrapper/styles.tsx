import { StyleSheet } from "react-native"

export const useStyles=()=>{

    const styles=StyleSheet.create({
        mainWrapper:{
            flex:1
        }
    })

    return {
        styles
    }
}